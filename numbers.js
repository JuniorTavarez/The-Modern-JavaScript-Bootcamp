let age = 26;
let dogYear = (age + 1) / 7;

console.log(dogYear);

const studentScore = 18;

const maxScore = 20;

const percent = (studentScore, maxScore) => {
  console.log((studentScore / maxScore) * 100);
};

percent(studentScore, maxScore);
